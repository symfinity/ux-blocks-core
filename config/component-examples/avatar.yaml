schema: showcase-examples/1.0
role: avatar
twig_name: Avatar
requires_stimulus: false
sections:
  - id: semantic
    title: Semantic variants
    layout: stack
    items:
      - variant: base
        label: Base (no variant)
        props: { variant: '', size: default }
      - variant: primary
        label: Primary
        props: { variant: primary, size: default }
      - variant: secondary
        label: Secondary
        props: { variant: secondary, size: default }
      - variant: tertiary
        label: Tertiary
        props: { variant: tertiary, size: default }
      - variant: destructive
        label: Destructive
        props: { variant: destructive, size: default }
      - variant: success
        label: Success
        props: { variant: success, size: default }
      - variant: info
        label: Info
        props: { variant: info, size: default }
      - variant: warning
        label: Warning
        props: { variant: warning, size: default }
  - id: sizes
    title: Sizes
    layout: stack
    items:
      - variant: sm
        label: Small
        props: { variant: primary, size: sm }
      - variant: lg
        label: Large
        props: { variant: primary, size: lg }
