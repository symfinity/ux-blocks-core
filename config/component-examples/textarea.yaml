schema: showcase-examples/1.0
role: textarea
twig_name: Textarea
requires_stimulus: false
sections:
  - id: states
    title: States
    layout: stack
    items:
      - variant: default
        label: Default
        props: { placeholder: Notes }
      - variant: disabled
        label: Disabled
        props: { disabled: true }
        slot_text: Disabled content
      - variant: invalid
        label: Invalid
        props: { invalid: true }
        slot_text: Invalid content
