# radio-group

**Role:** `radio-group`  
**Fragment id:** `blocks.radio-group`  
**Nested item fragment:** `blocks.radio-group.item`

Compose with `RadioGroup:Item` children. Each item **must** use the same `name` as sibling items (and typically matches the group `name` prop).
