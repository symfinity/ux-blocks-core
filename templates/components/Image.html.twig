<img
    src="{{ src }}"
    alt="{{ alt }}"
    loading="{{ loading }}"
    decoding="async"
    data-ui-role="image"
    data-ui-fragment="blocks.image"
    data-ui-variant="{{ variant }}"
    {{ attributes }}
>
