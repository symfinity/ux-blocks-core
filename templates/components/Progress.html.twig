<progress
    data-ui-role="progress"
    data-ui-fragment="blocks.progress"
    data-ui-variant="{{ semantic_variant }}"
    value="{{ value }}"
    max="{{ max }}"
    {{ attributes }}
>{% block content %}{% endblock %}</progress>