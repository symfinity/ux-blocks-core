<input
    data-ui-role="switch"
    data-ui-fragment="blocks.switch"
    data-ui-variant="{{ semantic_variant }}"
    type="checkbox"
    role="switch"
    {% if checked %}checked{% endif %}
    {% if disabled %}disabled data-ui-state="disabled"{% endif %}
    {{ attributes }}
/>